diff --git a/crates/zeroclaw-runtime/src/security/bubblewrap.rs b/crates/zeroclaw-runtime/src/security/bubblewrap.rs
index c22e67649..66047933e 100644
--- a/crates/zeroclaw-runtime/src/security/bubblewrap.rs
+++ b/crates/zeroclaw-runtime/src/security/bubblewrap.rs
@@ -13,12 +13,18 @@ struct BubblewrapHardeningSupport {

 /// Bubblewrap sandbox backend
 #[derive(Debug, Clone, Default)]
-pub struct BubblewrapSandbox;
+pub struct BubblewrapSandbox {
+    workspace_dir: Option<std::path::PathBuf>,
+}

 impl BubblewrapSandbox {
     pub fn new() -> std::io::Result<Self> {
+        Self::with_workspace(None)
+    }
+
+    pub fn with_workspace(workspace_dir: Option<std::path::PathBuf>) -> std::io::Result<Self> {
         if Self::is_installed() {
-            Ok(Self)
+            Ok(Self{ workspace_dir })
         } else {
             Err(std::io::Error::new(
                 std::io::ErrorKind::NotFound,
@@ -130,6 +136,9 @@ impl BubblewrapSandbox {
                 bwrap_cmd.args(["--ro-bind", optional_dir, optional_dir]);
             }
         }
+        if let Some(ws) = &self.workspace_dir {
+            bwrap_cmd.args(["--bind".into(), ws.clone().into_os_string(), ws.clone().into_os_string()]);
+        }
         bwrap_cmd.arg(&program);
         bwrap_cmd.args(&args);

diff --git a/crates/zeroclaw-runtime/src/security/detect.rs b/crates/zeroclaw-runtime/src/security/detect.rs
index cf271dfa1..e7fa33ad2 100644
--- a/crates/zeroclaw-runtime/src/security/detect.rs
+++ b/crates/zeroclaw-runtime/src/security/detect.rs
@@ -315,9 +315,11 @@ fn create_selected_sandbox(
                 any(target_os = "linux", target_os = "macos")
             ))]
             {
-                super::bubblewrap::BubblewrapSandbox::new()
-                    .map(|sandbox| Arc::new(sandbox) as Arc<dyn Sandbox>)
-                    .ok()
+                super::bubblewrap::BubblewrapSandbox::with_workspace(
+                    workspace_dir.map(Path::to_path_buf),
+                )
+                .map(|sandbox| Arc::new(sandbox) as Arc<dyn Sandbox>)
+                .ok()
             }
             #[cfg(not(all(
                 feature = "sandbox-bubblewrap",
